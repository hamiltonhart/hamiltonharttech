import { IconButtonStyle, NavIconButtonStyle } from "./styles/Buttons";

interface Props {
  children?: React.ReactNode
}

export const NavIconButton = ({ children }:Props) => {
  <NavIconButtonStyle>{children}</NavIconButtonStyle>;
};
